#ifndef MENUS_H
#define MENUS_H

#include "struct.h"
#include "var_global.h"
#include "sequences.h"

int baisser_position(int position);
int monter_position(int position, int max);
void reset();

#endif